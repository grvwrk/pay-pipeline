# Leave unset to use the Vite dev proxy (recommended for local dev).
# Set to an absolute origin when the frontend is served separately from the API,
# e.g. a static build behind nginx talking to a remote backend.
VITE_API_BASE=
